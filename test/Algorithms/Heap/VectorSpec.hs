module Algorithms.Heap.VectorSpec where

-- import Algorithms.Heap.Vector
import Test.Hspec
-- import Test.Hspec.QuickCheck
-- import Test.QuickCheck

spec :: Spec
spec = pure()
-- spec :: Spec
-- spec =
--   describe "indexing operations" $ do
--     prop "parent . left = id" $ \(NonNegative i) -> parent (left i) == i
--     prop "parent . right = id" $ \(NonNegative i) -> parent (right i) == i
--     prop "parent i < left i < right i" $ \(NonNegative i) ->
--       parent i < left i && left i < right i
